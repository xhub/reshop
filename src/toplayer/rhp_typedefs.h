#ifndef RHP_TYPEDEFS_H
#define RHP_TYPEDEFS_H

#include "reshop.h"

/* ----------------------------------------------------------------------
 * Forward declaration of enum is forbidden. Hence, we need to first include
 * reshop.h and then do the typedefs
 * ---------------------------------------------------------------------- */

typedef enum rhp_backendtype BackendType;

#endif
